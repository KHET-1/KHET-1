# Sample Evidence

Place pcap/pcapng files here for testing.

```bash
# Generate a sample capture:
sudo tcpdump -i any -c 100 -w samples/test_capture.pcap

# Or copy from evidence directory:
cp ../evidence/1.pcapng samples/
```
